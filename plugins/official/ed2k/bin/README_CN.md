# 可选的包内 aMule

离线发行时，可把 aMule 的完整可移植目录放在这里，并确保此目录或其 `bin/` 子目录同时包含 `amuled.exe` 与 `amulecmd.exe`。

插件也支持通过 `AMULE_HOME`、`AMULED_PATH`/`AMULECMD_PATH`、系统 `PATH` 或外部 EC 使用用户已有的 aMule。不要把来源不明的可执行文件放进官方插件包；发布包含 aMule 的离线包时，还必须保留上游许可证和归属文件。
