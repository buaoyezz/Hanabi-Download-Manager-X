# HDMX TODO List

## 图例

| 标签 | 说明 |
| ---- | ---- |
| `Optimize` | 优化改进 |
| `Feature` | 功能特性 |
| `DOCS` | 文档 |
| `TEST` | 测试 |
| `FIX` | Bug 修复 |

---

## 现存计划

**FIX**
- [ ] 修复下载文件报错500等问题
- [ ] 下载内核稳定性第二阶段
  目标：继续减少弱网环境下的失败率与误判，不再让单个分段错误放大成整任务崩溃
- [ ] 重写动态分段策略
  目标：从“按剩余大小拆分”改成“按真实吞吐/尾块抢占”决定是否拆分，避免伪动态分段
- [ ] 完善 Range / Resume 安全模型
  目标：区分“可并行下载”和“可安全恢复”，无 validator 时允许本次会话并行，但禁止不安全的跨重启并行恢复
- [ ] 完善失败降级链
  目标：失败时优先降并发，再降协议，再退单连接，避免直接清空进度重下
- [ ] 修复恢复下载时的分片保留与错误提示
  目标：服务器不再支持 Range 时保留已下载分片，并给出明确失败原因，而不是静默重置
- [ ] 梳理 HTTP 状态码与响应头处理
  目标：补齐 `200` / `206` / `416` / `5xx` 与 `Content-Range` / `ETag` / `Last-Modified` / `If-Range` 的边界逻辑

**Feature**
- [ ] 重写 Popup 弹窗 — 更换技术栈为 Flutter，方便集成，提升视觉和使用体验
- [ ] 增强下载稳定性 — 改进下载过程中的稳定性问题
- [ ] 修复下载多分段的并发问题

---

## 长期计划

**Optimize**

- [ ] 对下载稳定性和速度的改进
- [ ] 建立 Host 级下载策略缓存
  目标：按域名记忆更稳的并发数、协议偏好与失败降级结果，避免每次重新试错
- [ ] 建立弱网自适应并发控制
  目标：根据失败率、吞吐抖动、stall 时间动态收缩或放宽并发，而不是只靠固定线程数
- [ ] 优化大文件尾段收敛速度
  目标：减少“最后一个慢分段拖全局”的情况，提升大文件下载完成时间
- [ ] 对客户端流畅性的改进

**TEST**

- [ ] 增加下载内核策略测试
  目标：覆盖并行门槛、重试退避、并发降级、恢复失败保留分片等核心规则
- [ ] 增加弱网模拟测试
  目标：覆盖超时、抖动、间歇断连、半途返回 `200/206/416/5xx` 等场景
- [ ] 增加断点续传回归测试
  目标：覆盖多分片恢复、无 validator 恢复限制、服务器变更资源后的错误路径

---

## 未来计划

**Feature**

- [ ] 对 FireFox 浏览器的插件支持

---
